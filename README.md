# cloud-computing

this project is part of the exam for cloud computing
to run the project all needed is to run

- docker compose up
  to run the test:
  just run the bash file stress after statring all the containers
